spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: ms-cliente
              uri: lb://CLIENTS
              predicates:
                - Path=/api/clients/**
            - id: ms-autopartes
              uri: lb://AUTOPARTES
              predicates:
                - Path=/api/**
    loadbalancer:
      ribbon:
        enabled: 'false'
  application:
    name: api-gateway

eureka:
  client:
    service-url:
      defaultZone: https://ms-eureka-server.nxgen.dev/eureka/
    register-with-eureka: 'true'

server:
  port: '8080'
